# Queue

Diretório reservado para documentação e futuras implementações de workers externos. A lógica de fila síncrona da API fica em `services/queue_manager.py` para não conflitar com o módulo padrão `queue` do Python.
