# Deploy

## Render

Use o arquivo `backend/render.yaml`. Configure `ADMIN_PASSWORD` e, se usar Firebase Admin autenticado, `FIREBASE_SERVICE_ACCOUNT`.

## Vercel

Root Directory: `dashboard`.
Environment Variable: `VITE_API_URL=https://sua-api.onrender.com`.
